# Managed by Ansible
local   all             all                                     peer
host    all             all             127.0.0.1/32            md5
host    all             all             ::1/128                 md5
{% for address in postgresql_allowed_hosts %}
host    {{ postgresql_db_name }}   {{ postgresql_db_user }}   {{ address }}            md5
{% endfor %}
{% for entry in postgresql_additional_hba_entries %}
{{ entry }}
{% endfor %}
