### Managed by Ansible ###
Server={{ zabbix_proxy_server_ip }}
Hostname={{ zabbix_proxy_hostname }}
ListenIP={{ zabbix_proxy_listen_ip }}
ProxyMode={{ zabbix_proxy_mode | lower == 'passive' | ternary(1, 0) }}
DBName={{ zabbix_proxy_db_name }}
